import SectionWrapper from "../components/SectionWrapper";

const Quality = () => (
  <SectionWrapper title="Quality">
    Quality is non-negotiable for us...
  </SectionWrapper>
);

export default Quality;
