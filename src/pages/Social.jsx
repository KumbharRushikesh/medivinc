import SectionWrapper from "../components/SectionWrapper";

const Social = () => (
  <SectionWrapper title="Social Activities">
    We are committed to various social activities...
  </SectionWrapper>
);

export default Social;
